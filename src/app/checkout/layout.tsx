export default function CheckoutLayout({ children }: { children: React.ReactNode }) {
  const iosFullPagePayment=`(()=>{const isIOS=()=>/iPad|iPhone|iPod/i.test(navigator.userAgent)||(navigator.platform==='MacIntel'&&navigator.maxTouchPoints>1);if(!isIOS())return;const label=()=>{const lang=(document.documentElement.lang||'').toLowerCase();return lang.startsWith('he')?'המשך לתשלום מאובטח':lang.startsWith('ru')?'Продолжить к безопасной оплате':'Continue to secure payment'};const enhance=()=>{document.querySelectorAll('iframe').forEach((frame)=>{try{const src=frame.getAttribute('src')||'';if(!(src.includes('/payments/hyp/')||src.includes('hyp.co.il')||src.includes('creditguard.co.il')))return;if(frame.dataset.atlasIosFullPage==='1')return;frame.dataset.atlasIosFullPage='1';const button=document.createElement('button');button.type='button';button.textContent=label();button.setAttribute('aria-label',label());button.style.cssText='width:100%;min-height:56px;border:0;border-radius:12px;background:#000;color:#fff;font:600 17px/1.2 -apple-system,BlinkMacSystemFont,Segoe UI,sans-serif;padding:16px 20px;cursor:pointer;margin:10px 0;box-shadow:none;';button.addEventListener('click',()=>{const target=frame.src||src;if(target)window.location.assign(target);});frame.style.display='none';frame.parentElement?.insertBefore(button,frame); }catch{}})};enhance();new MutationObserver(enhance).observe(document.documentElement,{childList:true,subtree:true,attributes:true,attributeFilter:['src']});})();`;
  return <>{children}<script dangerouslySetInnerHTML={{__html:iosFullPagePayment}}/></>;
}
