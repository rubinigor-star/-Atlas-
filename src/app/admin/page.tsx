import Link from "next/link"; import {db} from "@/lib/db"; import {money,eventDate} from "@/lib/format"; import {AdminShell} from "@/components/admin-shell";
export const dynamic="force-dynamic";
export default async function Admin(){const [events,orders,revenue,used]=await Promise.all([db.event.findMany({include:{venue:true,categories:true},orderBy:{startsAt:"asc"}}),db.order.findMany({take:6,orderBy:{createdAt:"desc"},include:{event:true,tickets:true}}),db.order.aggregate({_sum:{totalMinor:true},where:{status:"PAID"}}),db.ticket.count({where:{status:"USED"}})]);return <AdminShell><div className="row between"><div><span className="eyebrow">Organizer back-office</span><h1>Панель управления</h1></div><Link href="/admin/events/new" className="btn">+ Новое событие</Link></div><div className="stats"><div className="stat"><span className="muted">Продажи</span><strong>{money(revenue._sum.totalMinor??0)}</strong></div><div className="stat"><span className="muted">Заказов</span><strong>{orders.length}</strong></div><div className="stat"><span className="muted">Прошли контроль</span><strong>{used}</strong></div></div><h2 className="section-title">Мероприятия</h2><div className="table-wrap"><table><thead><tr><th>Событие</th><th>Дата</th><th>Статус</th><th>Продано</th></tr></thead><tbody>{events.map(e=><tr key={e.id}><td><strong>{e.title}</strong><br/><small>{e.venue.name}</small></td><td>{eventDate(e.startsAt)}</td><td><span className="pill">{e.status}</span></td><td>{e.categories.reduce((s,c)=>s+c.sold,0)} / {e.categories.reduce((s,c)=>s+c.capacity,0)}</td></tr>)}</tbody></table></div><div className="row between"><h2 className="section-title">Последние заказы</h2><Link href="/admin/orders">Все заказы →</Link></div><div className="table-wrap"><table><thead><tr><th>Заказ</th><th>Покупатель</th><th>Сумма</th><th>Статус</th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td><Link href={`/admin/orders/${o.publicId}`}><strong>{o.publicId}</strong></Link></td><td>{o.customerName}<br/><small>{o.customerEmail}</small></td><td>{money(o.totalMinor)}</td><td>{o.status}</td></tr>)}</tbody></table></div></AdminShell>}
