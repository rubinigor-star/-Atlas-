"use client";
import Link from "next/link";
import {ReactNode,useState} from "react";
import {useLocale} from "@/components/locale-provider";

const copy={ru:{portal:"Портал промоутера",home:"Главная",events:"Мероприятия",sales:"Продажи",profile:"Профиль",logout:"Выйти"},he:{portal:"פורטל פרומוטרים",home:"ראשי",events:"אירועים",sales:"מכירות",profile:"פרופיל",logout:"יציאה"},en:{portal:"Promoter Portal",home:"Home",events:"Events",sales:"Sales",profile:"Profile",logout:"Sign out"}} as const;
export function PromoterPortalShell({name,organization,children}:{name:string;organization:string;children:ReactNode}){const{locale}=useLocale();const t=copy[locale];const[loading,setLoading]=useState(false);async function logout(){setLoading(true);await fetch('/api/promoter/auth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'logout'})});window.location.assign('/promoter/login');}return <div style={{minHeight:'100vh',background:'#f5f7fa',color:'#101828',fontFamily:'Arial,sans-serif'}}><header style={{background:'#081426',color:'white',padding:'16px 24px'}}><div style={{maxWidth:1180,margin:'0 auto',display:'flex',alignItems:'center',justifyContent:'space-between',gap:20,flexWrap:'wrap'}}><div><div style={{fontWeight:800,fontSize:20}}>Atlas One</div><div style={{fontSize:12,opacity:.65}}>{t.portal} · {organization}</div></div><nav style={{display:'flex',gap:16,alignItems:'center',flexWrap:'wrap'}}><Link style={{color:'white',textDecoration:'none'}} href='/promoter'>{t.home}</Link><Link style={{color:'white',textDecoration:'none'}} href='/promoter/events'>{t.events}</Link><Link style={{color:'white',textDecoration:'none'}} href='/promoter/sales'>{t.sales}</Link><Link style={{color:'white',textDecoration:'none'}} href='/promoter/profile'>{t.profile}</Link><span style={{opacity:.75}}>{name}</span><button onClick={logout} disabled={loading} style={{border:'1px solid rgba(255,255,255,.25)',background:'transparent',color:'white',borderRadius:8,padding:'8px 12px',cursor:'pointer'}}>{loading?'...':t.logout}</button></nav></div></header><main style={{maxWidth:1180,margin:'0 auto',padding:'28px 20px 60px'}}>{children}</main></div>}
