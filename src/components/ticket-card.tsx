import Image from "next/image"; import Link from "next/link";
export function TicketCard({ticket,qr}:{ticket:{id:string;publicCode:string;status:string;category:{name:string}},qr:string}){return <article className="panel" style={{marginTop:18}}><div className="row between"><div><span className="pill">{ticket.status}</span><h3>{ticket.category.name}</h3></div><code>{ticket.publicCode.slice(0,14)}…</code></div><Image src={qr} alt="Ticket QR code" width={240} height={240} className="qr" unoptimized/><div className="row" style={{justifyContent:"center"}}><Link className="btn secondary" href={`/api/tickets/${ticket.id}/pdf`}>Скачать PDF</Link></div></article>}
