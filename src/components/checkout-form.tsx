"use client";
import {useState} from "react"; import {useRouter} from "next/navigation"; import {money} from "@/lib/format";
export function CheckoutForm({eventId,categoryId,quantity,tableId,total,title,label}:{eventId:string;categoryId:string;quantity:number;tableId?:string;total:number;title:string;label:string}){const router=useRouter();const [busy,setBusy]=useState(false);const [error,setError]=useState("");const [promo,setPromo]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const form=new FormData(e.currentTarget);const res=await fetch("/api/checkout",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({eventId,categoryId,quantity,tableId:tableId||null,promoCode:promo||undefined,customer:{name:form.get("name"),email:form.get("email"),phone:form.get("phone")},idempotencyKey:crypto.randomUUID()})});const data=await res.json();if(!res.ok){setError(data.error??"Не удалось оформить заказ");setBusy(false);return}router.push(`/orders/${data.orderId}`)}return <form onSubmit={submit} className={`checkout ${busy?"loading":""}`}><section><span className="eyebrow">Checkout</span><h1>Контактные данные</h1><div className="panel form"><div className="field"><label>Имя и фамилия</label><input className="input" name="name" required minLength={2} autoComplete="name"/></div><div className="field"><label>Телефон</label><input className="input" name="phone" required autoComplete="tel" placeholder="+972..."/></div><div className="field"><label>Email для билетов</label><input className="input" name="email" required type="email" autoComplete="email"/></div><div className="field"><label>Промокод</label><div className="row"><input className="input" value={promo} onChange={e=>setPromo(e.target.value.toUpperCase())} placeholder="Например, ATLAS10"/></div></div><div className="panel" style={{background:"#f8fafc"}}><strong>Тестовая оплата</strong><p className="muted">Данные карты не требуются. Нажатие кнопки создаст демонстрационный оплаченный заказ.</p></div>{error&&<div className="toast">{error}</div>}<button className="btn" disabled={busy}>{busy?"Оформляем...":"Подтвердить тестовый заказ"}</button></div></section><aside className="panel summary"><span className="pill">Ваш заказ</span><h2>{title}</h2><p>{label}</p><div className="row between"><span className="muted">Количество</span><strong>{quantity}</strong></div><hr style={{border:0,borderTop:"1px solid #e5e7eb",margin:"18px 0"}}/><div className="row between"><strong>Итого</strong><strong style={{fontSize:25}}>{money(total)}</strong></div></aside></form>}
