if (process.env.VERCEL_ENV !== 'preview') { console.log('[HYP CG probe] skipped outside preview'); process.exit(0); }
const terminal=(process.env.HYP_MASOF||'').trim(); const user=(process.env.HYP_API_KEY||'').trim(); const password=(process.env.HYP_PASSP||'').trim();
if(!terminal||!user||!password){console.log('[HYP CG probe] missing HYP vars');process.exit(0);}
const endpoints=['https://cg.creditguard.co.il/xpo/Relay','https://cguat2.creditguard.co.il/xpo/Relay','https://cgpay.creditguard.co.il/xpo/Relay'];
const esc=v=>v.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&apos;');
const xml=`<ashrait><request><version>2000</version><language>Eng</language><dateTime/><requestId/><command>getSessionId</command><getSessionId><terminalNumber>${esc(terminal)}</terminalNumber></getSessionId></request></ashrait>`;
const extract=(text,tag)=>text.match(new RegExp(`<${tag}(?:\\s[^>]*)?>([\\s\\S]*?)<\\/${tag}>`,'i'))?.[1]?.trim()||'';
for(const endpoint of endpoints){const host=new URL(endpoint).host;try{const r=await fetch(endpoint,{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded','user-agent':'Atlas-One-CreditGuard-Probe/1.0'},body:new URLSearchParams({user,password,int_in:xml}),redirect:'manual',signal:AbortSignal.timeout(12000)});const text=(await r.text()).slice(0,20000);console.log('[HYP CG probe]',JSON.stringify({host,httpStatus:r.status,result:extract(text,'result')||null,message:extract(text,'message')||extract(text,'userMessage')||null,additionalInfo:extract(text,'additionalInfo')||null,hasSessionId:Boolean(extract(text,'sessionId')),responseType:text.trim().startsWith('<')?'xml':'other'}));}catch(error){console.log('[HYP CG probe]',JSON.stringify({host,error:error instanceof Error?error.message:'probe failed'}));}}
